import {AssetManager} from "@/util/AssetManager";
import {Islam} from "@/util/Islam";
import {Constants} from "@/util/Constants";

export const ClientUtils = {
    AssetManager,
    Islam,
    Constants,
} as const;