import {syne} from "@/fonts";

const ScriptiumText = () => {

    return <h1 className={syne.className + " text-2xl transition-opacity group-hover:opacity-80"}>Scriptium</h1>;
}

export default ScriptiumText;