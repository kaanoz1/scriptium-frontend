"use client";
import { NextPage } from "next";
import SignUpForm from "./components/SignUpForm";

type Props = object;

const Page: NextPage<Props> = ({}) => {
  return <SignUpForm />;
};

export default Page;
