"use client";
import SignUpForm from "@/components/SignUpForm";
import { NextPage } from "next";

type Props = object;

const Page: NextPage<Props> = ({}) => {
  return <SignUpForm />;
};

export default Page;
